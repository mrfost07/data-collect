# ============================================
# Face Data Collector - Environment Variables
# ============================================
# Copy this file to .env.local and fill in your values

# API Upload Key (required, server-only — NOT prefixed with NEXT_PUBLIC_)
# Generate a random string for upload authentication
API_UPLOAD_KEY=your-random-api-key-here

# Allowed origins for CORS (comma-separated)
# Required for production — add your Vercel domain
# Example: ALLOWED_ORIGINS=https://your-app.vercel.app,https://custom-domain.com
ALLOWED_ORIGINS=

# ============================================
# Cloudinary Configuration
# ============================================
# Sign up at https://cloudinary.com (free tier: 25GB storage, 25GB bandwidth/month)
# Find these values in your Cloudinary Dashboard → Settings → Access Keys

CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret
